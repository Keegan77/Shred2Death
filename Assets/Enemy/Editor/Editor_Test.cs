using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Editor_Test
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    public void tingle () { return; }
    // Update is called once per frame
    void Update()
    {
        
    }
}
